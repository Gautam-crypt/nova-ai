// Placeholder for Local ONNX inference (Phase 5 offline engine)
// Uses onnxruntime-react-native for running small models directly on device.

export const processLocally = async (text: string) => {
    // This will load a quantized local model later
    return "Local processing not fully implemented yet.";
};
